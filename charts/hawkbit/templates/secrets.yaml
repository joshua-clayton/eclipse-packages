{{- if not .Values.auth.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "hawkbit.fullname" . }}-user
  labels:
{{ include "hawkbit.labels" . | indent 4 }}
type: Opaque
stringData:
  application-user-credentials.yaml: |-
    hawkbit:
      security:
        user:
          {{ .Values.auth.username }}:
            password: {{ .Values.auth.password | quote }}
---
{{- end }}
{{- if and (not .Values.externalDatabase.existingSecret) (not .Values.mariadb.enabled) .Values.externalDatabase.host }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "hawkbit.fullname" . }}-db
  labels:
{{ include "hawkbit.labels" . | indent 4 }}
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: {{ include "hawkbit.database.user" . | quote }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.externalDatabase.password | quote }}
---
{{- end }}
{{- if and .Values.hawkbitgui.enabled (not .Values.hawkbitgui.nextauth.existingSecret) .Values.hawkbitgui.nextauth.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "hawkbit.fullname" . }}-gui
  labels:
{{ include "hawkbit.labels" . | indent 4 }}
type: Opaque
stringData:
  NEXTAUTH_SECRET: {{ .Values.hawkbitgui.nextauth.secret | quote }}
---
{{- end }}
{{- if and .Values.rabbitmq.enabled (not .Values.rabbitmq.credentialsSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "hawkbit.fullname" . }}-rabbitmq-creds
  labels:
{{ include "hawkbit.labels" . | indent 4 }}
type: Opaque
stringData:
  SPRING_RABBITMQ_USERNAME: {{ .Values.rabbitmq.auth.username | quote }}
  SPRING_RABBITMQ_PASSWORD: {{ .Values.rabbitmq.auth.password | quote }}
{{- end }}
