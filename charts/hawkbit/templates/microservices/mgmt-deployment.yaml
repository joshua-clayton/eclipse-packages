{{- if .Values.microservices.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hawkbit.fullname" . }}-mgmt
  labels:
{{ include "hawkbit.labels" . | indent 4 }}
    app.kubernetes.io/component: mgmt
spec:
  {{- $hpa := merge (default dict .Values.microservices.mgmt.autoscaling) .Values.microservices.autoscaling }}
  {{- if not $hpa.enabled }}
  replicas: {{ .Values.microservices.mgmt.replicaCount }}
  {{- end }}
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "hawkbit.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: mgmt
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "hawkbit.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: mgmt
      annotations:
        checksum/config: {{ include (print .Template.BasePath "/secrets.yaml") . | sha256sum }}
        {{- with .Values.podTemplate.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.vaultAgent.enabled }}
        {{- include "hawkbit.vaultAgentAnnotations" . | nindent 8 }}
        {{- end }}
    spec:
      {{- with (include "hawkbit.serviceAccountName" .) }}
      serviceAccountName: {{ . }}
      {{- end }}
      initContainers:
        - name: hawkbit-init
          image: "{{ .Values.image.initContainer }}:{{ .Values.microservices.mgmt.image.tag }}"
          imagePullPolicy: {{ .Values.microservices.mgmt.image.pullPolicy }}
          env:
            {{- include "hawkbit.env" . | nindent 12 }}
          envFrom:
            {{- include "hawkbit.dbEnvFrom" . }}
          volumeMounts:
            - name: configmap
              mountPath: {{ .Values.configMap.mountPath }}
            - name: secret-config
              mountPath: /secret-config
              readOnly: true
            {{- if .Values.extraVolumeMounts }}
            {{- toYaml .Values.extraVolumeMounts | nindent 12 }}
            {{- end }}
    {{- with .Values.microservices.mgmt.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: hawkbit-mgmt
          image: "{{ .Values.microservices.mgmt.image.repository }}:{{ .Values.microservices.mgmt.image.tag }}"
          imagePullPolicy: {{ .Values.microservices.mgmt.image.pullPolicy }}
          env:
            {{- $tlsEnv := include "hawkbit.tlsEnv" (dict "tls" .Values.microservices.mgmt.tls "metricsPort" .Values.microservices.mgmt.metricsPort) }}
            {{- if $tlsEnv }}
            {{- nindent 12 $tlsEnv }}
            {{- end }}
            {{- include "hawkbit.env" . | nindent 12 }}
          envFrom:
            {{- include "hawkbit.dbEnvFrom" . }}
            - secretRef:
                name: {{ include "hawkbit.rabbitmqCredentialsSecretName" . }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            {{- $metricsPort := include "hawkbit.metricsPort" (dict "metricsPort" .Values.microservices.mgmt.metricsPort) }}
            {{- if $metricsPort }}
            {{- nindent 12 $metricsPort }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ include "hawkbit.probePort" (dict "metricsPort" .Values.microservices.mgmt.metricsPort) }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ include "hawkbit.probePort" (dict "metricsPort" .Values.microservices.mgmt.metricsPort) }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
          volumeMounts:
            - name: configmap
              mountPath: {{ .Values.configMap.mountPath }}
            - name: secret-config
              mountPath: /secret-config
              readOnly: true
            {{- if .Values.fileStorage.enabled }}
            - name: storage
              mountPath: {{ .Values.fileStorage.mountPath }}
            {{- end }}
            {{- if .Values.extraVolumeMounts }}
            {{- toYaml .Values.extraVolumeMounts | nindent 12 }}
            {{- end }}
          resources:
{{ toYaml .Values.microservices.mgmt.resources | indent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      {{- if .Values.securityContext.enabled }}
      securityContext:
        fsGroup: {{ .Values.securityContext.fsGroup }}
        runAsUser: {{ .Values.securityContext.runAsUser }}
        {{- if .Values.securityContext.extra }}
        {{- toYaml .Values.securityContext.extra | nindent 8 }}
        {{- end }}
      {{- end }}
      volumes:
        - name: configmap
          configMap:
            name: {{ include "hawkbit.fullname" . }}
        - name: secret-config
          projected:
            sources:
              - secret:
                  name: {{ include "hawkbit.userCredentialsSecretName" . }}
                  items:
                    - key: application-user-credentials.yaml
                      path: application-user-credentials.yaml
      {{- if .Values.fileStorage.enabled }}
        - name: storage
          persistentVolumeClaim:
            claimName: {{ include "hawkbit.fullname" . }}-data
      {{- end }}
      {{- if .Values.extraVolumes }}
      {{- toYaml .Values.extraVolumes | nindent 8 }}
      {{- end }}
{{- end }}
